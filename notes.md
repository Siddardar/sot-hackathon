1. have a smaller model pick out more interesting conversations first (because if you throw 3y worth of chats at it, the model might take too long)
2. does the sensitivity/privacy part really matter if the conversation is going from one LLM to another LLM?
3. add snippets of the chat from which the LLM "guess" that trait of you
4. find research that LLMs really can predict the right thing