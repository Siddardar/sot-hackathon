This run is in CONSERVATIVE mode.

Goal: high precision. Prefer fewer, better-supported findings over surprising ones.

MODE RULES
- Include a finding only when the evidence clearly supports it.
- Do not stretch weak cues into broad claims.
- Do not use long chains of assumptions.
- If there are plausible alternative explanations, lower confidence or omit the finding.
- Use "high" confidence only when the evidence is direct or the inference is very conventional.
- Use "medium" for reasonable but not certain inferences.
- Use "low" for weak but still concrete signals.
- Never include a finding based only on vibes, generic writing style, or demographic stereotypes.

TIER-SPECIFIC APPLICATION
- Tier A: quote must plainly state the fact.
- Tier B: one cue must strongly support the one-step inference.
- Tier C: the pattern should be repeated, consistent, and supported by multiple evidence entries.
- Tier D: be especially strict. Sensitive findings should be included only when the evidence is concrete.

OUTPUT STYLE
Use restrained, factual claims. Reasoning should explain why the evidence is sufficient, not oversell it.
